image: freebsd/13.x
packages:
  - gmake
  - llvm
  - binutils
  - valgrind
  - coreutils
sources:
  - https://git.sr.ht/~jprotopopov/kefir
tasks:
  - test: |
      cd kefir
      gmake test PLATFORM=freebsd MEMCHECK=yes OPT=-O3 SANITIZE=undefined CC=clang
  - build: |
      cd kefir
      gmake clean clean_bootstrap
      gmake all PLATFORM=freebsd OPT=-O3 CC=clang
  - bootstrap: |
      cd kefir
      gmake clean clean_bootstrap
      export LD_LIBRARY_PATH="$LD_LIBRARY_PATH:$HOME/kefir/bin/libs"
      export KEFIR_RTLIB="$HOME/kefir/bin/libs/libkefirrt.a"
      export KEFIR_RTINC="$HOME/kefir/headers/kefir/runtime"
      export KEFIR_FREEBSD_INCLUDE="/usr/include;/usr/local/include"
      export KEFIR_FREEBSD_LIB="/usr/lib;/usr/local/lib"
      export KEFIR_FREEBSD_DYNAMIC_LINKER="/libexec/ld-elf.so.1"
      export REALPATH=grealpath
      LC_ALL=C.UTF-8 gmake bootstrap KEFIR_EXTRAFLAGS="--target opt-x86_64-host-gnu -O1" CC=clang PLATFORM=freebsd
triggers:
  - action: email
    condition: failure
    to: jevgenij@protopopov.lv
