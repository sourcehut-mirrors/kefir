.att_syntax
.section .note.GNU-stack,"",%progbits

.global or
.type or, @function
.global and
.type and, @function

.section .text
.L__kefir_text_section_begin:
or:
.L__kefir_text_func_or_begin:
    jmp .L__kefir_func_or_label1
.L__kefir_func_or_label1:
    or %esi, %edi
    setne %dil
    movzx %dil, %rdi
    or %esi, %edi
    setne %dil
    movzx %dil, %rdi
    or %esi, %edi
    setne %dil
    movzx %dil, %rdi
    mov %rdi, %rax
    ret
.L__kefir_text_func_or_end:

and:
.L__kefir_text_func_and_begin:
    jmp .L__kefir_func_and_label1
.L__kefir_func_and_label1:
    test %edi, %edi
    setne %al
    test %esi, %esi
    setne %cl
    and %al, %cl
    movzx %cl, %rax
    ret
.L__kefir_text_func_and_end:

.L__kefir_text_section_end:

