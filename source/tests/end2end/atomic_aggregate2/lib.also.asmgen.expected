.att_syntax
.section .note.GNU-stack,"",%progbits

.global load_buffer
.type load_buffer, @function
.global store_buffer
.type store_buffer, @function

.section .text
.L__kefir_text_section_begin:
load_buffer:
.L__kefir_text_func_load_buffer_begin:
    push %rbp
    mov %rsp, %rbp
    push %rbx
    sub $72, %rsp
    mov %rdi, %rbx
    leaq -72(%rbp), %rdx
    mov $64, %rdi
    mov $5, %rcx
    call __atomic_load@PLT
    mov %rbx, %rax
    movdqu -72(%rbp), %xmm0
    movdqu %xmm0, (%rax)
    movdqu -56(%rbp), %xmm0
    movdqu %xmm0, 16(%rax)
    movdqu -40(%rbp), %xmm0
    movdqu %xmm0, 32(%rax)
    movdqu -24(%rbp), %xmm0
    movdqu %xmm0, 48(%rax)
    lea -8(%rbp), %rsp
    pop %rbx
    pop %rbp
    ret
.L__kefir_text_func_load_buffer_end:

store_buffer:
.L__kefir_text_func_store_buffer_begin:
    mov %rsi, %rax
    mov %rdi, %rsi
    mov %rax, %rdx
    mov $64, %rdi
    mov $5, %rcx
    call __atomic_store@PLT
    ret
.L__kefir_text_func_store_buffer_end:

.L__kefir_text_section_end:

