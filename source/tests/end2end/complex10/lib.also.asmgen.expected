.att_syntax
.section .note.GNU-stack,"",%progbits

.global test32
.type test32, @function

.section .text
__kefir_text_section_begin:
test32:
__kefir_text_func_test32_begin:
    push %rbp
    mov %rsp, %rbp
    push %rbx
    sub $24, %rsp
    stmxcsr -16(%rbp)
    jmp __kefir_func_test32_label1
__kefir_func_test32_label1:
    movaps %xmm0, %xmm1
    movaps %xmm1, %xmm2
    shufps $1, %xmm1, %xmm2
    movd %xmm0, %eax
    movd %xmm2, %ecx
    shl $32, %rcx
    or %rax, %rcx
mov %rcx, %rbx
mov %rcx, %rax
shr $32, %rax
shl $32, %rbx
or %rbx, %rax

    mov %rax, -24(%rbp)
    movd -24(%rbp), %xmm0
    movd -20(%rbp), %xmm1
    insertps $16, %xmm1, %xmm0
    ldmxcsr -16(%rbp)
    lea -8(%rbp), %rsp
    pop %rbx
    pop %rbp
    ret
__kefir_text_func_test32_end:

__kefir_text_section_end:

