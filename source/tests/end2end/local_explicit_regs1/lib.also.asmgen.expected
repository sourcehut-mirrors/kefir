.att_syntax
.section .note.GNU-stack,"",%progbits

.global testfn
.type testfn, @function

.section .text
__kefir_text_section_begin:
testfn:
__kefir_text_func_testfn_begin:
    push %rbp
    mov %rsp, %rbp
    sub $16, %rsp
    mov %rsi, %rax
    mov %rdi, %rcx
    movq $0, -8(%rbp)
    mov %rdx, %rdi
    mov %rcx, %rsi
    mov %rax, %rdx
call somefn
    mov %rax, -8(%rbp)
    movq -8(%rbp), %rax
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_testfn_end:

__kefir_text_section_end:

