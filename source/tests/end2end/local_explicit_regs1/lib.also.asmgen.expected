.att_syntax
.section .note.GNU-stack,"",%progbits

.global testfn
.type testfn, @function

.section .text
.L__kefir_text_section_begin:
testfn:
.L__kefir_text_func_testfn_begin:
    push %rbp
    mov %rsp, %rbp
    sub $16, %rsp
    jmp .L__kefir_func_testfn_label1
.L__kefir_func_testfn_label1:
    mov %rsi, %rax
    mov %rdi, %rcx
    movq $0, -8(%rbp)
    mov %rdx, %rdi
    mov %rcx, %rsi
    mov %rax, %rdx
call somefn
    mov %rax, -8(%rbp)
    movq -8(%rbp), %rax
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_testfn_end:

.L__kefir_text_section_end:

