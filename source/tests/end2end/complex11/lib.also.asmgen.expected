.att_syntax
.section .note.GNU-stack,"",%progbits

.global test32
.type test32, @function
.global test64
.type test64, @function

.section .text
__kefir_text_section_begin:
test32:
__kefir_text_func_test32_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movaps %xmm0, %xmm1
    shufps $1, %xmm1, %xmm1
    movd %xmm0, %eax
    movd %xmm1, %ecx
    shl $32, %rcx
    or %rax, %rcx
    movq %rcx, %xmm1
movaps %xmm1, %xmm0
shufps $1, %xmm0, %xmm0
    movq %xmm0, -16(%rbp)
    movd -16(%rbp), %xmm0
    movd -12(%rbp), %xmm1
    insertps $16, %xmm1, %xmm0
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_test32_end:

test64:
__kefir_text_func_test64_begin:
    push %rbp
    mov %rsp, %rbp
    sub $48, %rsp
    stmxcsr -8(%rbp)
    movaps %xmm1, %xmm2
    movaps %xmm0, %xmm1
    shufpd $0, %xmm2, %xmm1
movapd %xmm1, %xmm0
shufpd $1, %xmm0, %xmm0
    movdqu %xmm0, -24(%rbp)
    movq -24(%rbp), %xmm0
    movq -16(%rbp), %xmm1
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_test64_end:

__kefir_text_section_end:

