.att_syntax
.section .note.GNU-stack,"",%progbits

.global test32
.type test32, @function
.global test64
.type test64, @function

.section .text
.L__kefir_text_section_begin:
test32:
.L__kefir_text_func_test32_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movaps %xmm0, %xmm1
    movaps %xmm1, %xmm2
    shufps $1, %xmm1, %xmm2
    movaps %xmm0, %xmm3
    movd %xmm3, %eax
    movd %xmm2, %ecx
    shl $32, %rcx
    or %rax, %rcx
    movq %rcx, %xmm1
movaps %xmm1, %xmm0
shufps $1, %xmm0, %xmm0
    movq %xmm0, -16(%rbp)
    movd -16(%rbp), %xmm0
    movd -12(%rbp), %xmm1
    insertps $16, %xmm1, %xmm0
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_test32_end:

test64:
.L__kefir_text_func_test64_begin:
    push %rbp
    mov %rsp, %rbp
    sub $48, %rsp
    stmxcsr -8(%rbp)
    movaps %xmm0, %xmm2
    movaps %xmm1, %xmm3
    movaps %xmm2, %xmm1
    shufpd $0, %xmm3, %xmm1
movapd %xmm1, %xmm0
shufpd $1, %xmm0, %xmm0
    movdqu %xmm0, -24(%rbp)
    movq -24(%rbp), %xmm0
    movq -16(%rbp), %xmm1
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_test64_end:

.L__kefir_text_section_end:

