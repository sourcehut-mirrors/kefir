.att_syntax
.section .note.GNU-stack,"",%progbits

.global x
.type x, @object
.global get
.type get, @function

.section .text
__kefir_text_section_begin:
get:
__kefir_text_func_get_begin:
__kefir_func_get_label0:
    push %rbp
    mov %rsp, %rbp
    sub $16, %rsp
    jmp __kefir_func_get_label1
__kefir_func_get_label1:
    movl %edi, -4(%rbp)
    movl -4(%rbp), %ecx
    mov %ecx, %eax
    mov %rax, %rdx
    add %eax, %edx
    sar $31, %ecx
    mov %rdx, %rax
    xor %ecx, %eax
    or $1, %eax
    mov %rax, %rcx
    bsr %eax, %ecx
    mov %rcx, %rax
    xor $31, %eax
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_get_end:

__kefir_text_section_end:

.section .data
    .align 4
x:
    .long 30

