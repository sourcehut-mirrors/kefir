.att_syntax
.section .note.GNU-stack,"",%progbits

.extern x
.global addx
.type addx, @function

.section .text
.L__kefir_text_section_begin:
addx:
.L__kefir_text_func_addx_begin:
    push %rbp
    mov %rsp, %rbp
    sub $16, %rsp
    movq x@GOTPCREL(%rip), %rcx
    movq (%rcx), %rax
    movq %rax, -8(%rbp)
.L__kefir_func_addx_label2:
    movq -8(%rbp), %rax
    movq x@GOTPCREL(%rip), %rdx
    mov %rax, %rsi
    add %rdi, %rsi
    mov %rsi, %rcx
    movq -8(%rbp), %rax
    lock     cmpxchgq %rcx, (%rdx)
    je .L__kefir_func_addx_label4
    movq %rax, -8(%rbp)
.L__kefir_func_addx_label4:
    jnz .L__kefir_func_addx_label2
    mov %rsi, %rax
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_addx_end:

.L__kefir_text_section_end:

