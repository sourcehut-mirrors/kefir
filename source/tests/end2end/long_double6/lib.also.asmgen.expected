.att_syntax
.section .note.GNU-stack,"",%progbits

.global init_s
.type init_s, @function

.section .text
.L__kefir_text_section_begin:
init_s:
.L__kefir_text_func_init_s_begin:
    push %rbp
    mov %rsp, %rbp
    sub $16, %rsp
    fstcw -8(%rbp)
    movsx %esi, %rax
    push %rax
    fildq (%rsp)
    add $8, %rsp
    movq $0, 8(%rdi)
    fstpt (%rdi)
    mov %edx, %eax
    push %rax
    fildq (%rsp)
    add $8, %rsp
    test %rax, %rax
    jns .L__kefir_func_init_s_label3
    fadds .L__kefir_constant_uint_to_long_double(%rip)
.L__kefir_func_init_s_label3:
    movq $0, 24(%rdi)
    fstpt 16(%rdi)
    movq %xmm0, -16(%rbp)
    fldl -16(%rbp)
    movq $0, 40(%rdi)
    fstpt 32(%rdi)
    mov %rdi, %rax
    fldcw -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_init_s_end:

.L__kefir_text_section_end:

.section .rodata
    .align 16
.L__kefir_constant_uint_to_long_double:
    .long 1602224128
