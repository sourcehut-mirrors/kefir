.att_syntax
.section .note.GNU-stack,"",%progbits

.global hypot_sq
.type hypot_sq, @function

.section .text
.L__kefir_text_section_begin:
hypot_sq:
.L__kefir_text_func_hypot_sq_begin:
    push %rbp
    mov %rsp, %rbp
    sub $16, %rsp
    fstcw -8(%rbp)
    jmp .L__kefir_func_hypot_sq_label1
.L__kefir_func_hypot_sq_label1:
    fldt 32(%rbp)
    fldt 16(%rbp)
    fld %st(0)
    fmulp
    fxch %st(1)
    fld %st(0)
    fmulp
    fxch %st(1)
    faddp
    fldcw -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_hypot_sq_end:

.L__kefir_text_section_end:

