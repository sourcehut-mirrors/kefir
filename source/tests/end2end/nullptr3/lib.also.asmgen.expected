.att_syntax
.section .note.GNU-stack,"",%progbits

.global sum
.type sum, @function

.section .text
__kefir_text_section_begin:
sum:
__kefir_text_func_sum_begin:
    push %rbp
    mov %rsp, %rbp
    sub $208, %rsp
    test %al, %al
    je __kefir_func_sum_label2
    jmp __kefir_func_sum_label1
__kefir_func_sum_label1:
    movdqu %xmm0, -160(%rbp)
    movdqu %xmm1, -144(%rbp)
    movdqu %xmm2, -128(%rbp)
    movdqu %xmm3, -112(%rbp)
    movdqu %xmm4, -96(%rbp)
    movdqu %xmm5, -80(%rbp)
    movdqu %xmm6, -64(%rbp)
    movdqu %xmm7, -48(%rbp)
    jmp __kefir_func_sum_label2
__kefir_func_sum_label2:
    mov %rdi, -208(%rbp)
    mov %rsi, -200(%rbp)
    mov %rdx, -192(%rbp)
    mov %rcx, -184(%rbp)
    mov %r8, -176(%rbp)
    mov %r9, -168(%rbp)
    jmp __kefir_func_sum_label3
__kefir_func_sum_label3:
    movl $0, -32(%rbp)
    movl $48, -28(%rbp)
    lea 16(%rbp), %rax
    movq %rax, -24(%rbp)
    lea -208(%rbp), %rax
    movq %rax, -16(%rbp)
    xor %eax, %eax
    mov %rax, %rsi
    jmp __kefir_func_sum_label4
__kefir_func_sum_label4:
    movl -32(%rbp), %ecx
    cmp $48, %ecx
    jae __kefir_func_sum_label6
    jmp __kefir_func_sum_label5
__kefir_func_sum_label5:
    lea 8(%rcx), %rcx
    movl %ecx, -32(%rbp)
    add -16(%rbp), %rcx
    mov -8(%rcx), %rcx
    mov %rcx, %rdx
    jmp __kefir_func_sum_label7
__kefir_func_sum_label6:
    mov -24(%rbp), %rcx
    lea 8(%rcx), %rcx
    mov %rcx, -24(%rbp)
    mov -8(%rcx), %rcx
    mov %rcx, %rdx
    jmp __kefir_func_sum_label7
__kefir_func_sum_label7:
    lea N(%rip), %rcx
    movq (%rcx), %rcx
    cmp %rcx, %rdx
    je __kefir_func_sum_label11
    jmp __kefir_func_sum_label9
__kefir_func_sum_label9:
    movq (%rdx), %rcx
    mov %rsi, %rax
    add %rcx, %rax
    mov %rax, %rsi
    jmp __kefir_func_sum_label4
__kefir_func_sum_label11:
    mov %rsi, %rax
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_sum_end:

__kefir_text_section_end:

.section .data
    .align 8
N:
    .quad 0

