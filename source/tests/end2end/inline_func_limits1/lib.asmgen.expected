.att_syntax
.section .note.GNU-stack,"",%progbits

.extern A
.global main
.type main, @function

.section .text
.L__kefir_text_section_begin:
main:
.L__kefir_text_func_main_begin:
    push %rbp
    mov %rsp, %rbp
    movq A@GOTPCREL(%rip), %rax
    movl (%rax), %ecx
    lea 100(%ecx), %edx
    movl %edx, (%rax)
    movq A@GOTPCREL(%rip), %rax
    lea 50(%ecx), %edx
    movl %edx, (%rax)
    movq A@GOTPCREL(%rip), %rax
    lea 250(%ecx), %edx
    movl %edx, (%rax)
    movq A@GOTPCREL(%rip), %rax
    lea 220(%ecx), %edx
    movl %edx, (%rax)
    movq A@GOTPCREL(%rip), %rax
    lea -280(%ecx), %edx
    movl %edx, (%rax)
    movq A@GOTPCREL(%rip), %rax
    lea -325(%ecx), %edx
    movl %edx, (%rax)
    movq A@GOTPCREL(%rip), %rax
    lea -202(%ecx), %edx
    movl %edx, (%rax)
    movq A@GOTPCREL(%rip), %rax
    movl %edx, (%rax)
    movq A@GOTPCREL(%rip), %rax
    lea -292(%ecx), %edx
    movl %edx, (%rax)
    movq A@GOTPCREL(%rip), %rax
    add $-236, %ecx
    movl %ecx, (%rax)
    mov $1000, %edi
    call test1
    mov $2000, %edi
    pop %rbp
    jmp test2
.L__kefir_text_func_main_end:

test1:
.L__kefir_text_func_test1_begin:
    movq A@GOTPCREL(%rip), %rax
    movl (%rax), %ecx
    add %edi, %ecx
    movl %ecx, (%rax)
    ret
.L__kefir_text_func_test1_end:

test2:
.L__kefir_text_func_test2_begin:
    movq A@GOTPCREL(%rip), %rax
    movl (%rax), %ecx
    sub %edi, %ecx
    movl %ecx, (%rax)
    ret
.L__kefir_text_func_test2_end:

.L__kefir_text_section_end:

