.att_syntax
.section .note.GNU-stack,"",%progbits

.global custom_discriminant
.type custom_discriminant, @function

.section .text
.L__kefir_text_section_begin:
custom_discriminant:
.L__kefir_text_func_custom_discriminant_begin:
.L__kefir_func_custom_discriminant_label0:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
.L__kefir_func_custom_discriminant_label1:
    movq %xmm2, -16(%rbp)
    movq %xmm1, -24(%rbp)
    movq %xmm0, -32(%rbp)
    movq %xmm0, -8(%rbp)
    movq -24(%rbp), %rax
    movq -32(%rbp), %rcx
    movq -16(%rbp), %rdx
    movq %rax, %xmm1
    movq %rcx, %xmm2
    movq %rdx, %xmm3
mulsd %xmm1, %xmm1
movq %xmm2, %xmm0
mulsd %xmm3, %xmm0
addsd %xmm0, %xmm0
addsd %xmm0, %xmm0
subsd %xmm0, %xmm1
    movq %xmm1, -8(%rbp)
    movq -8(%rbp), %rax
    movq %rax, %xmm0
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_custom_discriminant_end:

.L__kefir_text_section_end:

