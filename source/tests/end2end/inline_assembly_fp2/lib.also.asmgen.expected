.att_syntax
.section .note.GNU-stack,"",%progbits

.global custom_discriminant
.type custom_discriminant, @function

.section .text
.L__kefir_text_section_begin:
custom_discriminant:
.L__kefir_text_func_custom_discriminant_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    movq %xmm2, -32(%rbp)
    movq %xmm1, -24(%rbp)
    movq %xmm0, -8(%rbp)
    movq %xmm0, -16(%rbp)
    movq -24(%rbp), %xmm0
    movq -8(%rbp), %xmm4
    movq -32(%rbp), %xmm5
    movaps %xmm0, %xmm1
    movaps %xmm4, %xmm2
    movaps %xmm5, %xmm3
mulsd %xmm1, %xmm1
movq %xmm2, %xmm0
mulsd %xmm3, %xmm0
addsd %xmm0, %xmm0
addsd %xmm0, %xmm0
subsd %xmm0, %xmm1
    movq %xmm1, -16(%rbp)
    movq -16(%rbp), %xmm0
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_custom_discriminant_end:

.L__kefir_text_section_end:

