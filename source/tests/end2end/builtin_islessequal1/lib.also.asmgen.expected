.att_syntax
.section .note.GNU-stack,"",%progbits

.global f32
.type f32, @object
.global f64
.type f64, @object
.global f80
.type f80, @object
.global is_lessequal_f32
.type is_lessequal_f32, @function
.global is_lessequal_f64
.type is_lessequal_f64, @function
.global is_lessequal_f80
.type is_lessequal_f80, @function

.section .text
.L__kefir_text_section_begin:
is_lessequal_f32:
.L__kefir_text_func_is_lessequal_f32_begin:
    push %rbp
    mov %rsp, %rbp
    sub $16, %rsp
    stmxcsr -8(%rbp)
    movd %xmm1, -16(%rbp)
    movd %xmm0, -12(%rbp)
    movd -12(%rbp), %xmm0
    movd -16(%rbp), %xmm1
    xor %eax, %eax
    ucomiss %xmm0, %xmm1
    setnb %al
    movzx %al, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_is_lessequal_f32_end:

is_lessequal_f64:
.L__kefir_text_func_is_lessequal_f64_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movq %xmm1, -24(%rbp)
    movq %xmm0, -16(%rbp)
    movq -16(%rbp), %rax
    movq -24(%rbp), %rcx
    xor %edx, %edx
    movq %rcx, %xmm0
    movq %rax, %xmm1
    ucomisd %xmm1, %xmm0
    setnb %dl
    mov %rdx, %rax
    and $255, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_is_lessequal_f64_end:

is_lessequal_f80:
.L__kefir_text_func_is_lessequal_f80_begin:
    push %rbp
    mov %rsp, %rbp
    sub $48, %rsp
    fstcw -8(%rbp)
    fldt 32(%rbp)
    fldt 16(%rbp)
    movq $0, -40(%rbp)
    fxch %st(1)
    fstpt -48(%rbp)
    movq $0, -24(%rbp)
    fstpt -32(%rbp)
    fldt -32(%rbp)
    fldt -48(%rbp)
    xor %eax, %eax
    fucomi %st(1)
    setnb %al
    movzx %al, %eax
    fstp %st(0)
    fstp %st(0)
    fldcw -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_is_lessequal_f80_end:

.L__kefir_text_section_end:

.section .data
    .align 16
f32:
    .long 0
    .long 1
    .long 0
    .long 1
    .long 1
    .long 1

    .align 16
f64:
    .long 0
    .long 1
    .long 0
    .long 1
    .long 1
    .long 1

    .align 16
f80:
    .long 0
    .long 1
    .long 0
    .long 1
    .long 1
    .long 1

