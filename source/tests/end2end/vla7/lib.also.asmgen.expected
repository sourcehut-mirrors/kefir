.att_syntax
.section .note.GNU-stack,"",%progbits

.global test
.type test, @function
.global test2
.type test2, @function

.section .text
.L__kefir_text_section_begin:
test:
.L__kefir_text_func_test_begin:
    push %rbp
    mov %rsp, %rbp
    sub $48, %rsp
    xor %eax, %eax
    movl %edi, -32(%rbp)
    sub $16, %rsp
    movq %rax, (%rsp)
    mov %rsp, %rcx
    lea -48(%rbp), %rdx
    movq %rax, (%rdx)
    movl -32(%rbp), %eax
    movsx %eax, %rax
    shl $2, %rax
    movq %rax, -16(%rbp)
    sub %rax, %rsp
    mov $16, %rax
    cmp $4, %rax
    mov $4, %rdx
    cmovl %rdx, %rax
    neg %rax
    and %rax, %rsp
    mov %rsp, %rax
    movq %rax, -24(%rbp)
    movq -24(%rbp), %rax
    movq %rax, -8(%rbp)
.L__kefir_func_test_label2:
    movl -32(%rbp), %eax
    add $-1, %eax
    movl %eax, -32(%rbp)
    lea -24(%rbp), %rax
    movq (%rax), %rax
    movq -8(%rbp), %rdx
    cmp %rdx, %rax
    jnz .L__kefir_func_test_label6
    movl -32(%rbp), %eax
    cmp $1, %eax
    jg .L__kefir_func_test_label2
    mov $1, %eax
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_func_test_label6:
    xor %eax, %eax
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_test_end:

test2:
.L__kefir_text_func_test2_begin:
    push %rbp
    mov %rsp, %rbp
    sub $64, %rsp
    xor %eax, %eax
    movl %edi, -48(%rbp)
    sub $16, %rsp
    movq %rax, (%rsp)
    mov %rsp, %rcx
    lea -64(%rbp), %rdx
    movq %rax, (%rdx)
    movl -48(%rbp), %eax
    movsx %eax, %rax
    shl $2, %rax
    movq %rax, -32(%rbp)
    sub %rax, %rsp
    mov $16, %rax
    cmp $4, %rax
    mov $4, %rdx
    cmovl %rdx, %rax
    neg %rax
    and %rax, %rsp
    mov %rsp, %rax
    movq %rax, -40(%rbp)
.L__kefir_func_test2_label2:
    sub $16, %rsp
    movq %rcx, (%rsp)
    mov %rsp, %rax
    lea -64(%rbp), %rdx
    movq %rcx, 8(%rdx)
    movl -48(%rbp), %edx
    lea -24(%rbp), %rcx
    movsx %edx, %rdx
    shl $2, %rdx
    movq %rdx, 8(%rcx)
    lea -24(%rbp), %rcx
    sub %rdx, %rsp
    mov $16, %rdx
    cmp $4, %rdx
    mov $4, %rsi
    cmovl %rsi, %rdx
    neg %rdx
    and %rdx, %rsp
    mov %rsp, %rdx
    movq %rdx, (%rcx)
    cmp $0, %rax
    jz .L__kefir_func_test2_label6
    mov %rax, %rsp
    movq (%rsp), %rcx
    add $16, %rsp
    jmp .L__kefir_func_test2_label2
.L__kefir_func_test2_label6:
    mov %rax, %rcx
    jmp .L__kefir_func_test2_label2
.L__kefir_text_func_test2_end:

.L__kefir_text_section_end:

