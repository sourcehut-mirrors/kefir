.att_syntax
.section .note.GNU-stack,"",%progbits

.global less_or_equalsld
.type less_or_equalsld, @function
.global greater_or_equalsd
.type greater_or_equalsd, @function
.global less_or_equalsd
.type less_or_equalsd, @function
.global greater_or_equalsld
.type greater_or_equalsld, @function

.section .text
.L__kefir_text_section_begin:
less_or_equalsld:
.L__kefir_text_func_less_or_equalsld_begin:
    push %rbp
    mov %rsp, %rbp
    sub $112, %rsp
    fstcw -8(%rbp)
    fldt 32(%rbp)
    fldt 16(%rbp)
    movq $0, -24(%rbp)
    fxch %st(1)
    fstpt -32(%rbp)
    movq $0, -40(%rbp)
    fstpt -48(%rbp)
    fldt -48(%rbp)
    fldt -32(%rbp)
    fxch %st(1)
    fxch %st(1)
    fld %st(0)
    fstpt -96(%rbp)
    fld %st(1)
    fstpt -112(%rbp)
    mov $0, %rax
    fcomip %st(1)
    fstp %st(0)
    seta %al
    fldt -112(%rbp)
    fldt -96(%rbp)
    fld %st(0)
    fstpt -96(%rbp)
    fld %st(1)
    fstpt -112(%rbp)
    mov $0, %rcx
    mov $0, %rdx
    fucomip %st(1)
    fstp %st(0)
    setnp %cl
    cmovne %rdx, %rcx
    or %rcx, %rax
    setne %al
    movzx %al, %rax
    fldcw -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_less_or_equalsld_end:

greater_or_equalsd:
.L__kefir_text_func_greater_or_equalsd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movq %xmm1, -16(%rbp)
    movq %xmm0, -24(%rbp)
    movq -24(%rbp), %rax
    movq -16(%rbp), %rcx
    mov $0, %rdx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    comisd %xmm1, %xmm0
    mov %rdx, %rax
    setae %al
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_greater_or_equalsd_end:

less_or_equalsd:
.L__kefir_text_func_less_or_equalsd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movq %xmm1, -16(%rbp)
    movq %xmm0, -24(%rbp)
    movq -24(%rbp), %rax
    movq -16(%rbp), %rcx
    mov $0, %rdx
    movq %rcx, %xmm0
    movq %rax, %xmm1
    comisd %xmm1, %xmm0
    mov %rdx, %rax
    setae %al
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_less_or_equalsd_end:

greater_or_equalsld:
.L__kefir_text_func_greater_or_equalsld_begin:
    push %rbp
    mov %rsp, %rbp
    sub $112, %rsp
    fstcw -8(%rbp)
    fldt 32(%rbp)
    fldt 16(%rbp)
    movq $0, -24(%rbp)
    fxch %st(1)
    fstpt -32(%rbp)
    movq $0, -40(%rbp)
    fstpt -48(%rbp)
    fldt -48(%rbp)
    fldt -32(%rbp)
    fxch %st(1)
    fld %st(0)
    fstpt -112(%rbp)
    fld %st(1)
    fstpt -96(%rbp)
    mov $0, %rax
    fcomip %st(1)
    fstp %st(0)
    seta %al
    fldt -112(%rbp)
    fldt -96(%rbp)
    fld %st(0)
    fstpt -96(%rbp)
    fld %st(1)
    fstpt -112(%rbp)
    mov $0, %rcx
    mov $0, %rdx
    fucomip %st(1)
    fstp %st(0)
    setnp %cl
    cmovne %rdx, %rcx
    or %rcx, %rax
    setne %al
    movzx %al, %rax
    fldcw -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_greater_or_equalsld_end:

.L__kefir_text_section_end:

