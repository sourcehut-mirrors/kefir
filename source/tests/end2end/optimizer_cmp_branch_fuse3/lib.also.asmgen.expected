.att_syntax
.section .note.GNU-stack,"",%progbits

.global lessd
.type lessd, @function
.global equalsd
.type equalsd, @function
.global greaterd
.type greaterd, @function
.global not_equalsd
.type not_equalsd, @function
.global greater_or_equalsd
.type greater_or_equalsd, @function
.global less_or_equalsd
.type less_or_equalsd, @function

.section .text
.L__kefir_text_section_begin:
lessd:
.L__kefir_text_func_lessd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movq %xmm1, -24(%rbp)
    movq %xmm0, -16(%rbp)
    movq -16(%rbp), %rax
    movq -24(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jp .L__kefir_func_lessd_label3
    jb .L__kefir_func_lessd_label4
.L__kefir_func_lessd_label3:
    xor %eax, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_func_lessd_label4:
    mov $1, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_lessd_end:

equalsd:
.L__kefir_text_func_equalsd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movq %xmm1, -24(%rbp)
    movq %xmm0, -16(%rbp)
    movq -16(%rbp), %rax
    movq -24(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jp .L__kefir_func_equalsd_label3
    je .L__kefir_func_equalsd_label4
.L__kefir_func_equalsd_label3:
    xor %eax, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_func_equalsd_label4:
    mov $1, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_equalsd_end:

greaterd:
.L__kefir_text_func_greaterd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movq %xmm1, -24(%rbp)
    movq %xmm0, -16(%rbp)
    movq -16(%rbp), %rax
    movq -24(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jp .L__kefir_func_greaterd_label3
    ja .L__kefir_func_greaterd_label4
.L__kefir_func_greaterd_label3:
    xor %eax, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_func_greaterd_label4:
    mov $1, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_greaterd_end:

not_equalsd:
.L__kefir_text_func_not_equalsd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movq %xmm1, -24(%rbp)
    movq %xmm0, -16(%rbp)
    movq -16(%rbp), %rax
    movq -24(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jp .L__kefir_func_not_equalsd_label4
    jne .L__kefir_func_not_equalsd_label4
    xor %eax, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_func_not_equalsd_label4:
    mov $1, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_not_equalsd_end:

greater_or_equalsd:
.L__kefir_text_func_greater_or_equalsd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movq %xmm1, -24(%rbp)
    movq %xmm0, -16(%rbp)
    movq -16(%rbp), %rax
    movq -24(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jp .L__kefir_func_greater_or_equalsd_label3
    jae .L__kefir_func_greater_or_equalsd_label4
.L__kefir_func_greater_or_equalsd_label3:
    xor %eax, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_func_greater_or_equalsd_label4:
    mov $1, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_greater_or_equalsd_end:

less_or_equalsd:
.L__kefir_text_func_less_or_equalsd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movq %xmm1, -24(%rbp)
    movq %xmm0, -16(%rbp)
    movq -16(%rbp), %rax
    movq -24(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jp .L__kefir_func_less_or_equalsd_label3
    jbe .L__kefir_func_less_or_equalsd_label4
.L__kefir_func_less_or_equalsd_label3:
    xor %eax, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_func_less_or_equalsd_label4:
    mov $1, %eax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_less_or_equalsd_end:

.L__kefir_text_section_end:

