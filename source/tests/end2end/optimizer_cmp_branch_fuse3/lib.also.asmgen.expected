.att_syntax
.section .note.GNU-stack,"",%progbits

.global lessd
.type lessd, @function
.global equalsd
.type equalsd, @function
.global greaterd
.type greaterd, @function
.global not_equalsd
.type not_equalsd, @function
.global greater_or_equalsd
.type greater_or_equalsd, @function
.global less_or_equalsd
.type less_or_equalsd, @function
.extern __kefirrt_fenv_update

.section .text
__kefir_text_section_begin:
lessd:
__kefir_text_func_lessd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
_kefir_func_lessd_label0:
_kefir_func_lessd_label1:
    movq %xmm1, -16(%rbp)
    movq %xmm0, -24(%rbp)
    movq -24(%rbp), %rax
    movq -16(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jb _kefir_func_lessd_label2
    jmp _kefir_func_lessd_label3
_kefir_func_lessd_label2:
    mov $1, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
_kefir_func_lessd_label3:
    mov $0, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_lessd_end:

equalsd:
__kefir_text_func_equalsd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
_kefir_func_equalsd_label0:
_kefir_func_equalsd_label1:
    movq %xmm1, -16(%rbp)
    movq %xmm0, -24(%rbp)
    movq -24(%rbp), %rax
    movq -16(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jp _kefir_func_equalsd_label4
    je _kefir_func_equalsd_label2
_kefir_func_equalsd_label4:
    jmp _kefir_func_equalsd_label3
_kefir_func_equalsd_label2:
    mov $1, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
_kefir_func_equalsd_label3:
    mov $0, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_equalsd_end:

greaterd:
__kefir_text_func_greaterd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
_kefir_func_greaterd_label0:
_kefir_func_greaterd_label1:
    movq %xmm1, -16(%rbp)
    movq %xmm0, -24(%rbp)
    movq -24(%rbp), %rax
    movq -16(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    ja _kefir_func_greaterd_label2
    jmp _kefir_func_greaterd_label3
_kefir_func_greaterd_label2:
    mov $1, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
_kefir_func_greaterd_label3:
    mov $0, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_greaterd_end:

not_equalsd:
__kefir_text_func_not_equalsd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
_kefir_func_not_equalsd_label0:
_kefir_func_not_equalsd_label1:
    movq %xmm1, -16(%rbp)
    movq %xmm0, -24(%rbp)
    movq -24(%rbp), %rax
    movq -16(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jp _kefir_func_not_equalsd_label2
    jne _kefir_func_not_equalsd_label2
    jmp _kefir_func_not_equalsd_label3
_kefir_func_not_equalsd_label2:
    mov $1, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
_kefir_func_not_equalsd_label3:
    mov $0, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_not_equalsd_end:

greater_or_equalsd:
__kefir_text_func_greater_or_equalsd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
_kefir_func_greater_or_equalsd_label0:
_kefir_func_greater_or_equalsd_label1:
    movq %xmm1, -16(%rbp)
    movq %xmm0, -24(%rbp)
    movq -24(%rbp), %rax
    movq -16(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jae _kefir_func_greater_or_equalsd_label2
    jmp _kefir_func_greater_or_equalsd_label3
_kefir_func_greater_or_equalsd_label2:
    mov $1, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
_kefir_func_greater_or_equalsd_label3:
    mov $0, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_greater_or_equalsd_end:

less_or_equalsd:
__kefir_text_func_less_or_equalsd_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
_kefir_func_less_or_equalsd_label0:
_kefir_func_less_or_equalsd_label1:
    movq %xmm1, -16(%rbp)
    movq %xmm0, -24(%rbp)
    movq -24(%rbp), %rax
    movq -16(%rbp), %rcx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    jbe _kefir_func_less_or_equalsd_label2
    jmp _kefir_func_less_or_equalsd_label3
_kefir_func_less_or_equalsd_label2:
    mov $1, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
_kefir_func_less_or_equalsd_label3:
    mov $0, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_less_or_equalsd_end:

__kefir_text_section_end:

