.att_syntax
.section .note.GNU-stack,"",%progbits

.global test32
.type test32, @function

.section .text
__kefir_text_section_begin:
test32:
__kefir_text_func_test32_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movaps %xmm0, %xmm1
    shufps $1, %xmm1, %xmm1
    leaq -16(%rbp), %rax
    movd %xmm0, %ecx
    movd %xmm1, %edx
    shl $32, %rdx
    or %rcx, %rdx
    mov %rdx, -24(%rbp)
movq -24(%rbp), %xmm0
shufps $1, %xmm0, %xmm0
movq %xmm0, (%rax)
    movd -16(%rbp), %xmm0
    movd -12(%rbp), %xmm1
    insertps $16, %xmm1, %xmm0
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_test32_end:

__kefir_text_section_end:

