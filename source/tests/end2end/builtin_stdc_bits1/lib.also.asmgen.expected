.att_syntax
.section .note.GNU-stack,"",%progbits

.global bit_ceil
.type bit_ceil, @function
.global bit_floor
.type bit_floor, @function
.global rotate_left
.type rotate_left, @function
.global rotate_right
.type rotate_right, @function

.section .text
.L__kefir_text_section_begin:
bit_ceil:
.L__kefir_text_func_bit_ceil_begin:
    cmp $1, %rdi
    jle .L__kefir_func_bit_ceil_label2
    sub $1, %rdi
    bsr %rdi, %rax
    xor $63, %eax
    mov $63, %ecx
    sub %eax, %ecx
    movsx %ecx, %rcx
    mov $2, %eax
    shl %cl, %rax
    ret
.L__kefir_func_bit_ceil_label2:
    mov $1, %eax
    ret
.L__kefir_text_func_bit_ceil_end:

bit_floor:
.L__kefir_text_func_bit_floor_begin:
    cmp $0, %rdi
    je .L__kefir_func_bit_floor_label2
    bsr %rdi, %rax
    xor $63, %eax
    mov $63, %ecx
    sub %eax, %ecx
    movsx %ecx, %rcx
    mov $1, %eax
    shl %cl, %rax
    ret
.L__kefir_func_bit_floor_label2:
    xor %eax, %eax
    ret
.L__kefir_text_func_bit_floor_end:

rotate_left:
.L__kefir_text_func_rotate_left_begin:
    movzx %sil, %rsi
    mov %rsi, %rax
    cdq
    mov $64, %ecx
    idiv %ecx
    mov %rdx, %rcx
    mov %rsi, %rax
    neg %eax
    cdq
    mov $64, %esi
    idiv %esi
    movsx %ecx, %rcx
    mov %rdi, %rax
    shl %cl, %rax
    movsx %edx, %rcx
    shr %cl, %rdi
    or %rdi, %rax
    ret
.L__kefir_text_func_rotate_left_end:

rotate_right:
.L__kefir_text_func_rotate_right_begin:
    movzx %sil, %rsi
    mov %rsi, %rax
    cdq
    mov $64, %ecx
    idiv %ecx
    mov %rdx, %rcx
    mov %rsi, %rax
    neg %eax
    cdq
    mov $64, %esi
    idiv %esi
    movsx %ecx, %rcx
    mov %rdi, %rax
    shr %cl, %rax
    movsx %edx, %rcx
    shl %cl, %rdi
    or %rdi, %rax
    ret
.L__kefir_text_func_rotate_right_end:

.L__kefir_text_section_end:

