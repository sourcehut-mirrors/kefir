.att_syntax
.section .note.GNU-stack,"",%progbits

.global bit_ceil
.type bit_ceil, @function
.global bit_floor
.type bit_floor, @function
.global rotate_left
.type rotate_left, @function
.global rotate_right
.type rotate_right, @function

.section .text
__kefir_text_section_begin:
bit_ceil:
__kefir_text_func_bit_ceil_begin:
    jmp __kefir_func_bit_ceil_label1
__kefir_func_bit_ceil_label1:
    cmp $1, %rdi
    jg __kefir_func_bit_ceil_label5
    jmp __kefir_func_bit_ceil_label2
__kefir_func_bit_ceil_label2:
    mov $1, %rax
    jmp __kefir_func_bit_ceil_label3
__kefir_func_bit_ceil_label5:
    sub $1, %rdi
    bsr %rdi, %rax
    xor $63, %eax
    mov $63, %rcx
    sub %eax, %ecx
    movsx %ecx, %rcx
    mov $2, %rax
    shl %cl, %rax
    jmp __kefir_func_bit_ceil_label3
__kefir_func_bit_ceil_label3:
    ret
__kefir_text_func_bit_ceil_end:

bit_floor:
__kefir_text_func_bit_floor_begin:
    jmp __kefir_func_bit_floor_label1
__kefir_func_bit_floor_label1:
    cmp $0, %rdi
    jne __kefir_func_bit_floor_label5
    jmp __kefir_func_bit_floor_label2
__kefir_func_bit_floor_label2:
    xor %eax, %eax
    jmp __kefir_func_bit_floor_label3
__kefir_func_bit_floor_label5:
    bsr %rdi, %rax
    xor $63, %eax
    mov $63, %rcx
    sub %eax, %ecx
    movsx %ecx, %rcx
    mov $1, %rax
    shl %cl, %rax
    jmp __kefir_func_bit_floor_label3
__kefir_func_bit_floor_label3:
    ret
__kefir_text_func_bit_floor_end:

rotate_left:
__kefir_text_func_rotate_left_begin:
    jmp __kefir_func_rotate_left_label1
__kefir_func_rotate_left_label1:
    mov %rdi, %r8
    movzx %sil, %rsi
    mov %rsi, %rax
    cdq
    mov $64, %rcx
    idiv %ecx
    movsx %edx, %rcx
    mov %r8, %rdi
    shl %cl, %rdi
    mov %rsi, %rax
    neg %eax
    cdq
    mov $64, %rcx
    idiv %ecx
    movsx %edx, %rcx
    mov %r8, %rax
    shr %cl, %rax
    or %rax, %rdi
    mov %rdi, %rax
    ret
__kefir_text_func_rotate_left_end:

rotate_right:
__kefir_text_func_rotate_right_begin:
    jmp __kefir_func_rotate_right_label1
__kefir_func_rotate_right_label1:
    mov %rdi, %r8
    movzx %sil, %rsi
    mov %rsi, %rax
    cdq
    mov $64, %rcx
    idiv %ecx
    movsx %edx, %rcx
    mov %r8, %rdi
    shr %cl, %rdi
    mov %rsi, %rax
    neg %eax
    cdq
    mov $64, %rcx
    idiv %ecx
    movsx %edx, %rcx
    mov %r8, %rax
    shl %cl, %rax
    or %rax, %rdi
    mov %rdi, %rax
    ret
__kefir_text_func_rotate_right_end:

__kefir_text_section_end:

