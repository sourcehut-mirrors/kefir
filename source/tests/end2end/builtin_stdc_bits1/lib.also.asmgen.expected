.att_syntax
.section .note.GNU-stack,"",%progbits

.global bit_ceil
.type bit_ceil, @function
.global bit_floor
.type bit_floor, @function
.global rotate_left
.type rotate_left, @function
.global rotate_right
.type rotate_right, @function

.section .text
.L__kefir_text_section_begin:
bit_ceil:
.L__kefir_text_func_bit_ceil_begin:
    cmp $1, %rdi
    jg .L__kefir_func_bit_ceil_label8
    mov $1, %rax
.L__kefir_func_bit_ceil_label3:
    ret
.L__kefir_func_bit_ceil_label8:
    sub $1, %rdi
    bsr %rdi, %rax
    xor $63, %eax
    mov $63, %rcx
    sub %eax, %ecx
    movsx %ecx, %rcx
    mov $2, %rax
    shl %cl, %rax
    jmp .L__kefir_func_bit_ceil_label3
.L__kefir_text_func_bit_ceil_end:

bit_floor:
.L__kefir_text_func_bit_floor_begin:
    cmp $0, %rdi
    jne .L__kefir_func_bit_floor_label8
    xor %eax, %eax
.L__kefir_func_bit_floor_label3:
    ret
.L__kefir_func_bit_floor_label8:
    bsr %rdi, %rax
    xor $63, %eax
    mov $63, %rcx
    sub %eax, %ecx
    movsx %ecx, %rcx
    mov $1, %rax
    shl %cl, %rax
    jmp .L__kefir_func_bit_floor_label3
.L__kefir_text_func_bit_floor_end:

rotate_left:
.L__kefir_text_func_rotate_left_begin:
    movzx %sil, %rsi
    mov %rsi, %rax
    cdq
    mov $64, %ecx
    idiv %ecx
    movsx %edx, %rcx
    mov %rdi, %r8
    shl %cl, %r8
    mov %rsi, %rax
    neg %eax
    cdq
    mov $64, %ecx
    idiv %ecx
    movsx %edx, %rcx
    shr %cl, %rdi
    mov %r8, %rax
    or %rdi, %rax
    ret
.L__kefir_text_func_rotate_left_end:

rotate_right:
.L__kefir_text_func_rotate_right_begin:
    movzx %sil, %rsi
    mov %rsi, %rax
    cdq
    mov $64, %ecx
    idiv %ecx
    movsx %edx, %rcx
    mov %rdi, %r8
    shr %cl, %r8
    mov %rsi, %rax
    neg %eax
    cdq
    mov $64, %ecx
    idiv %ecx
    movsx %edx, %rcx
    shl %cl, %rdi
    mov %r8, %rax
    or %rdi, %rax
    ret
.L__kefir_text_func_rotate_right_end:

.L__kefir_text_section_end:

