.att_syntax
.section .note.GNU-stack,"",%progbits

.global test1
.type test1, @function

.section .text
__kefir_text_section_begin:
test1:
__kefir_text_func_test1_begin:
    push %rbp
    mov %rsp, %rbp
    sub $352, %rsp
    stmxcsr -8(%rbp)
    mov %rdi, %rdx
    movq $0, (%rdx)
    mov %rdx, %rdi
    add $8, %rdi
    movd _kefir_func_test1_label1(%rip), %xmm0
    movd %xmm0, (%rdi)
    mov %rdx, %rdi
    add $16, %rdi
    movq _kefir_func_test1_label2(%rip), %xmm0
    movq %xmm0, (%rdi)
    leaq -336(%rbp), %rdi
    xor %eax, %eax
    mov $16, %rcx
    rep stosb
    movdqu -336(%rbp), %xmm0
    movdqu %xmm0, -320(%rbp)
    fldt -320(%rbp)
    fstpt -352(%rbp)
    mov %rdx, %rdi
    add $32, %rdi
    movq $0, 8(%rdi)
    fldt -352(%rbp)
    fstpt (%rdi)
    mov %rdx, %rdi
    add $48, %rdi
    movd _kefir_func_test1_label3(%rip), %xmm0
    movd %xmm0, -352(%rbp)
    movd %xmm0, -348(%rbp)
    movq -352(%rbp), %xmm0
    movq %xmm0, (%rdi)
    mov %rdx, %rdi
    add $56, %rdi
    movq _kefir_func_test1_label4(%rip), %xmm0
    movq %xmm0, -352(%rbp)
    movq %xmm0, -344(%rbp)
    movq -352(%rbp), %xmm0
    movq %xmm0, (%rdi)
    movq -344(%rbp), %xmm0
    movq %xmm0, 8(%rdi)
    leaq -304(%rbp), %rdi
    xor %eax, %eax
    mov $32, %rcx
    rep stosb
    mov %rdx, %rdi
    add $80, %rdi
    movq $0, 8(%rdi)
    movq $0, 24(%rdi)
    fldt -304(%rbp)
    fstpt (%rdi)
    fldt -288(%rbp)
    fstpt 16(%rdi)
    leaq -272(%rbp), %rdi
    xor %eax, %eax
    mov $128, %rcx
    rep stosb
    leaq -144(%rbp), %rdi
    leaq -272(%rbp), %rsi
    cld
    mov $16, %rcx
    rep movsq
    mov %rdx, %rdi
    add $112, %rdi
    leaq -144(%rbp), %rsi
    cld
    mov $16, %rcx
    rep movsq
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_test1_end:
.section .rodata
    .align 4
_kefir_func_test1_label1:
    .long 0
    .align 8
_kefir_func_test1_label2:
    .quad 0
    .align 4
_kefir_func_test1_label3:
    .long 0
    .align 8
_kefir_func_test1_label4:
    .quad 0
.section .text

__kefir_text_section_end:

