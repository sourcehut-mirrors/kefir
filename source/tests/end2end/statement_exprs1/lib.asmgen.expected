.att_syntax
.section .note.GNU-stack,"",%progbits

.global eval1
.type eval1, @function
.global eval2
.type eval2, @function

.section .text
__kefir_text_section_begin:
eval1:
__kefir_text_func_eval1_begin:
    jmp __kefir_func_eval1_label1
__kefir_func_eval1_label1:
    mov %rdi, %rax
    add $1, %eax
    add %edi, %eax
    ret
__kefir_text_func_eval1_end:

eval2:
__kefir_text_func_eval2_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    xor %eax, %eax
    jmp __kefir_func_eval2_label1
__kefir_func_eval2_label1:
    mov %rax, %rcx
    sub $16, %rsp
    movq %rax, (%rsp)
    mov %rsp, %rdx
    movq %rcx, -24(%rbp)
    movsx %edi, %rax
    imul $8, %rax, %rcx
    movq %rcx, -8(%rbp)
    sub %rcx, %rsp
    mov $16, %rcx
    cmp $8, %rcx
    mov $8, %rsi
    cmovl %rsi, %rcx
    neg %rcx
    and %rcx, %rsp
    mov %rsp, %rcx
    movq %rcx, -16(%rbp)
    movq -8(%rbp), %rsi
    movq -24(%rbp), %rcx
    cmp $0, %rdx
    jnz __kefir_func_eval2_label2
__kefir_func_eval2_label6:
    mov %rdx, %rcx
    jmp __kefir_func_eval2_label3
__kefir_func_eval2_label2:
    mov %rdx, %rsp
    movq (%rsp), %rcx
    add $16, %rsp
    jmp __kefir_func_eval2_label3
__kefir_func_eval2_label3:
    add %rsi, %rax
    lea (%rbp), %rsp
    pop %rbp
    ret
__kefir_text_func_eval2_end:

__kefir_text_section_end:

