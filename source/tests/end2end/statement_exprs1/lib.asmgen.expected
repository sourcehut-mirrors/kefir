.att_syntax
.section .note.GNU-stack,"",%progbits

.global eval1
.type eval1, @function
.global eval2
.type eval2, @function

.section .text
.L__kefir_text_section_begin:
eval1:
.L__kefir_text_func_eval1_begin:
    mov %rdi, %rax
    add $1, %eax
    add %edi, %eax
    ret
.L__kefir_text_func_eval1_end:

eval2:
.L__kefir_text_func_eval2_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    xor %eax, %eax
    mov %rax, %rcx
    sub $16, %rsp
    movq %rax, (%rsp)
    mov %rsp, %rdx
    movq %rcx, -24(%rbp)
    movsx %edi, %rax
    imul $8, %rax, %rcx
    movq %rcx, -8(%rbp)
    sub %rcx, %rsp
    mov $16, %rcx
    cmp $8, %rcx
    mov $8, %rsi
    cmovl %rsi, %rcx
    neg %rcx
    and %rcx, %rsp
    mov %rsp, %rcx
    movq %rcx, -16(%rbp)
    movq -8(%rbp), %rsi
    movq -24(%rbp), %rcx
    cmp $0, %rdx
    jnz .L__kefir_func_eval2_label2
.L__kefir_func_eval2_label6:
    mov %rdx, %rcx
    jmp .L__kefir_func_eval2_label3
.L__kefir_func_eval2_label2:
    mov %rdx, %rsp
    movq (%rsp), %rcx
    add $16, %rsp
.L__kefir_func_eval2_label3:
    add %rsi, %rax
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_eval2_end:

.L__kefir_text_section_end:

