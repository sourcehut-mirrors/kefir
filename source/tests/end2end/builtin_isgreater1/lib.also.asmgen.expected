.att_syntax
.section .note.GNU-stack,"",%progbits

.global f32
.type f32, @object
.global f64
.type f64, @object
.global f80
.type f80, @object
.global is_greater_f32
.type is_greater_f32, @function
.global is_greater_f64
.type is_greater_f64, @function
.global is_greater_f80
.type is_greater_f80, @function

.section .text
.L__kefir_text_section_begin:
is_greater_f32:
.L__kefir_text_func_is_greater_f32_begin:
    push %rbp
    mov %rsp, %rbp
    sub $16, %rsp
    stmxcsr -8(%rbp)
    movd %xmm1, -12(%rbp)
    movd %xmm0, -16(%rbp)
    movl -16(%rbp), %eax
    movl -12(%rbp), %ecx
    xor %edx, %edx
    movd %eax, %xmm0
    movd %ecx, %xmm1
    ucomiss %xmm1, %xmm0
    seta %dl
    mov %rdx, %rax
    and $255, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_is_greater_f32_end:

is_greater_f64:
.L__kefir_text_func_is_greater_f64_begin:
    push %rbp
    mov %rsp, %rbp
    sub $32, %rsp
    stmxcsr -8(%rbp)
    movq %xmm1, -16(%rbp)
    movq %xmm0, -24(%rbp)
    movq -24(%rbp), %rax
    movq -16(%rbp), %rcx
    xor %edx, %edx
    movq %rax, %xmm0
    movq %rcx, %xmm1
    ucomisd %xmm1, %xmm0
    seta %dl
    mov %rdx, %rax
    and $255, %rax
    ldmxcsr -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_is_greater_f64_end:

is_greater_f80:
.L__kefir_text_func_is_greater_f80_begin:
    push %rbp
    mov %rsp, %rbp
    sub $80, %rsp
    fstcw -8(%rbp)
    fldt 32(%rbp)
    fldt 16(%rbp)
    movq $0, -24(%rbp)
    fxch %st(1)
    fstpt -32(%rbp)
    movq $0, -40(%rbp)
    fstpt -48(%rbp)
    fldt -48(%rbp)
    fldt -32(%rbp)
    fxch %st(1)
    xor %eax, %eax
    fucomi %st(1)
    seta %al
    and $255, %rax
    fstp %st(0)
    fstp %st(0)
    fldcw -8(%rbp)
    lea (%rbp), %rsp
    pop %rbp
    ret
.L__kefir_text_func_is_greater_f80_end:

.L__kefir_text_section_end:

.section .data
    .align 16
f32:
    .long 1
    .long 0
    .long 1
    .long 0
    .long 0
    .long 0

    .align 16
f64:
    .long 1
    .long 0
    .long 1
    .long 0
    .long 0
    .long 0

    .align 16
f80:
    .long 1
    .long 0
    .long 1
    .long 0
    .long 0
    .long 0

