.att_syntax
.section .note.GNU-stack,"",%progbits

.global test1
.type test1, @function

.section .text
__kefir_text_section_begin:
test1:
__kefir_text_func_test1_begin:
    mov %rdi, %rcx
    imul %esi, %edx
    mov %rdx, %r8
    add $1, %r8d
    xor %r9d, %r9d
_kefir_func_test1_label1:
    cmp %esi, %r9d
    jl _kefir_func_test1_label3
    ret
_kefir_func_test1_label3:
    movsx %r9d, %rax
    imul $4, %rax, %rax
    mov %rcx, %rdi
    add %rax, %rdi
    imul $2, %r9d, %eax
    mov %r8, %rdx
    add %eax, %edx
    movl %edx, (%rdi)
    mov %r9, %rax
    add $1, %eax
    mov %rax, %r9
    jmp _kefir_func_test1_label1
__kefir_text_func_test1_end:

__kefir_text_section_end:

